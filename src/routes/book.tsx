import { createFileRoute } from "@tanstack/react-router";import { useServerFn } from "@tanstack/react-start";import { useState } from "react";import { ArrowRight,CheckCircle2,LockKeyhole } from "lucide-react";import { SiteShell } from "@/components/site/SiteShell";import { pageHead } from "@/components/site/PageMeta";import { submitEnquiry } from "@/lib/content.functions";import { Input } from "@/components/ui/input";import { Textarea } from "@/components/ui/textarea";import { Button } from "@/components/ui/button";
export const Route=createFileRoute("/book")({head:()=>pageHead("Book a Private Session — GULFWELLBEING","Request a private wellbeing session with Tarkan for discreet support across the Gulf or internationally."),component:Page});
const countries=['United Arab Emirates','Saudi Arabia','Qatar','Kuwait','Bahrain','Oman','Other'];
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block"><span className="mb-2 block text-xs uppercase tracking-[.12em] text-muted-foreground">{label}</span>{children}</label>}
function Page(){const send=useServerFn(submitEnquiry);const [state,setState]=useState<'idle'|'sending'|'sent'|'error'>('idle');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('sending');const f=new FormData(e.currentTarget);try{await send({data:{name:String(f.get('name')),email:String(f.get('email')),phone:String(f.get('phone')||''),country:String(f.get('country')),area_of_support:String(f.get('area')),session_format:String(f.get('format')),availability:String(f.get('availability')),contact_method:String(f.get('contact')),message:String(f.get('message')||'')}});setState('sent')}catch{setState('error')}}return <SiteShell><section className="page-hero"><div className="mx-auto grid max-w-[1440px] gap-16 px-6 py-20 lg:grid-cols-[.75fr_1.25fr] lg:px-12 lg:py-28"><div><p className="eyebrow text-gold">PRIVATE ENQUIRY</p><h1 className="mt-6 font-serif text-6xl leading-none md:text-7xl">Request a private session.</h1><p className="mt-7 max-w-lg text-lg leading-8 text-muted-foreground">Share only what feels appropriate. Your request will be reviewed personally before the next step is arranged.</p><div className="mt-10 border-t border-border pt-8"><p className="eyebrow">WHAT HAPPENS NEXT</p><ol className="mt-5 space-y-5 text-sm text-muted-foreground"><li><b className="me-3 font-serif text-xl text-gold">01</b>Your enquiry is reviewed privately.</li><li><b className="me-3 font-serif text-xl text-gold">02</b>You are contacted through your preferred method.</li><li><b className="me-3 font-serif text-xl text-gold">03</b>Availability and the most suitable format are discussed.</li></ol></div><p className="mt-10 flex items-center gap-2 text-sm"><LockKeyhole className="size-4 text-gold"/>Designed with discretion in mind.</p></div><div className="border border-border bg-card p-7 md:p-10">{state==='sent'?<div className="grid min-h-[500px] place-items-center text-center"><div><CheckCircle2 className="mx-auto size-10 text-gold"/><h2 className="mt-6 font-serif text-4xl">Your request has been received.</h2><p className="mt-4 text-muted-foreground">Thank you. The next step will be arranged through your preferred contact method.</p></div></div>:<form onSubmit={submit} className="grid gap-6 md:grid-cols-2"><Field label="Name"><Input name="name" required minLength={2}/></Field><Field label="Email"><Input name="email" type="email" required/></Field><Field label="Phone / WhatsApp"><Input name="phone" type="tel" placeholder="Include country code"/></Field><Field label="Country"><select name="country" required className="field"><option value="">Select country</option>{countries.map(c=><option key={c}>{c}</option>)}</select></Field><Field label="Area of support"><select name="area" required className="field"><option value="">Select an area</option><option>Personal wellbeing</option><option>Women’s wellbeing</option><option>Men’s wellbeing</option><option>Business & executive</option><option>VIP concierge</option><option>Relationships & family</option></select></Field><Field label="Preferred session format"><select name="format" required className="field"><option value="">Select format</option><option>Online</option><option>In person</option><option>Open to discussion</option></select></Field><Field label="Preferred contact method"><select name="contact" required className="field"><option>Email</option><option>Phone</option><option>WhatsApp</option></select></Field><Field label="Preferred availability"><Input name="availability" required placeholder="Days, times and time zone"/></Field><div className="md:col-span-2"><Field label="Message / additional information"><Textarea name="message" rows={5} maxLength={2000}/></Field></div>{state==='error'&&<p className="md:col-span-2 text-sm text-destructive">We could not send your request. Please review the details and try again.</p>}<Button type="submit" size="lg" disabled={state==='sending'} className="md:col-span-2 md:justify-self-start">{state==='sending'?'Sending request…':'Request a Private Session'}<ArrowRight/></Button></form>}</div></div></section></SiteShell>}
