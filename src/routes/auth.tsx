import { createFileRoute,useNavigate } from "@tanstack/react-router";import { useState } from "react";import { supabase } from "@/integrations/supabase/client";import { lovable } from "@/integrations/lovable";import { BrandMark } from "@/components/site/BrandMark";import { Input } from "@/components/ui/input";import { Button } from "@/components/ui/button";import { pageHead, pageHeadFor } from "@/components/site/PageMeta";
export const Route=createFileRoute("/auth")({head: (ctx) => pageHeadFor(ctx, "Owner Sign In — GULFWELLBEING","Secure owner access to the GULFWELLBEING content manager."),component:Auth});
function Auth(){const navigate=useNavigate();const [mode,setMode]=useState<'signin'|'signup'>('signin');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const email=String(f.get('email'));const password=String(f.get('password'));const result=mode==='signin'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password,options:{emailRedirectTo:window.location.origin+'/admin'}});setBusy(false);if(result.error){setError(result.error.message);return}if(mode==='signup'&&!result.data.session){setError('Check your email to confirm your account, then return to sign in.');return}navigate({to:'/admin'})}async function google(){sessionStorage.setItem('auth-next','/admin');const result=await lovable.auth.signInWithOAuth('google',{redirect_uri:window.location.origin+'/auth'});if(result.error)setError(result.error.message);else if(!result.redirected)navigate({to:'/admin'})}return <main className="grid min-h-screen place-items-center bg-primary px-6 text-primary-foreground"><div className="w-full max-w-md"><BrandMark/><div className="mt-12 border border-primary-foreground/15 p-8"><p className="eyebrow text-gold">OWNER ACCESS</p><h1 className="mt-4 font-serif text-4xl">{mode==='signin'?'Welcome back.':'Create owner access.'}</h1><form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm">Email<Input name="email" type="email" required className="mt-2 bg-primary-foreground text-foreground"/></label><label className="block text-sm">Password<Input name="password" type="password" minLength={8} required className="mt-2 bg-primary-foreground text-foreground"/></label>{error&&<p className="text-sm text-gold">{error}</p>}<Button type="submit" variant="secondary" className="w-full" disabled={busy}>{busy?'Please wait…':mode==='signin'?'Sign in':'Create account'}</Button></form><div className="my-5 flex items-center gap-3 text-xs text-primary-foreground/40"><span className="h-px flex-1 bg-primary-foreground/15"/>OR<span className="h-px flex-1 bg-primary-foreground/15"/></div><Button type="button" variant="outline" onClick={google} className="w-full border-primary-foreground/25 bg-transparent text-primary-foreground">Continue with Google</Button><button type="button" onClick={()=>setMode(mode==='signin'?'signup':'signin')} className="mt-6 w-full text-center text-xs text-primary-foreground/60">{mode==='signin'?'First-time owner? Create account':'Already have access? Sign in'}</button></div></div></main>}
